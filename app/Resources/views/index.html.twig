<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Horse Racing Simulator{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
        <a href="{{ path('create_race') }}" value="Create Race"/>Create Race</a>
        {% if message is defined %}
            {{message}}
        {% endif %}
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
