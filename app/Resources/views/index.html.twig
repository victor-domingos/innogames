<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>Horse Racing Simulator</title>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <!--CREATE RACE-->
        <a href="{{ path('create_race') }}" value="Create Race"/>Create Race</a>
        {% if createRaceMsg is defined %}
            {{createRaceMsg}}
        {% endif %}
        <hr/>
        <!--PROGRESS-->
        <a href="{{ path('progress_race') }}" value="Progress">Progress</a>
        {% if progressRaceMsg is defined %}
            {{progressRaceMsg}}
        {% endif %}
        <hr/>
        <!--RUNNING RACES-->
        {% include 'running_races.html.twig' %}
        <hr/>
        <!--LAST 5 FINISHED RACES-->
        {% include 'finished_races.html.twig' %}
        <hr/>
        <!--RECORD-->
        {% include 'race_record.html.twig' %}
    </body>
</html>
